// project import
import pages from './pages';
import dashboard from './dashboard';
import utilities from './utilities';
import services from './services';

const menuItems = {
    items: [dashboard, utilities, services]
};

export default menuItems;
